export function LoadingState({rows=4,label='Carregando informações...'}:{rows?:number;label?:string}){return <div className="loading-state" role="status" aria-label={label}><div className="skeleton skeleton-title"/>{Array.from({length:rows}).map((_,i)=><div className="skeleton skeleton-row" key={i}/>)}</div>}
export function EmptyState({title,description,action}:{title:string;description:string;action?:React.ReactNode}){return <div className="empty-state polished-empty"><div className="empty-state-mark">V</div><h3>{title}</h3><p>{description}</p>{action&&<div className="empty-state-action">{action}</div>}</div>}
