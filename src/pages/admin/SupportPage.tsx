import { useEffect,useState } from 'react';
import { collection,doc,getDoc,getDocs,query,where } from 'firebase/firestore';
import { ExternalLink,Package,ShoppingCart,Users } from 'lucide-react';
import { Link,useParams } from 'react-router-dom';
import { db } from '../../lib/firebase'; import { Store } from '../../types/models';
export function SupportPage(){const {storeId}=useParams();const [store,setStore]=useState<Store|null>(null);const [counts,setCounts]=useState({products:0,orders:0,users:0});useEffect(()=>{if(!storeId)return;(async()=>{const s=await getDoc(doc(db,'stores',storeId));setStore(s.exists()?({id:s.id,...s.data()} as Store):null);const [p,o,u]=await Promise.all([getDocs(query(collection(db,'products'),where('storeId','==',storeId))),getDocs(query(collection(db,'orders'),where('storeId','==',storeId))),getDocs(query(collection(db,'users'),where('storeId','==',storeId)))]);setCounts({products:p.size,orders:o.size,users:u.size})})()},[storeId]);if(!store)return <div className="screen-center">Carregando loja...</div>;return <><div className="page-head"><div><Link className="back-link" to="/admin/lojas">← Voltar para lojas</Link><h1>Suporte · {store.name}</h1><p>Visão administrativa sem utilizar a senha do cliente.</p></div><a className="secondary-btn" href={`/loja/${store.slug}`} target="_blank" rel="noreferrer">Abrir vitrine <ExternalLink size={16}/></a></div><div className="stats"><div className="stat"><span><Package size={15}/> Produtos</span><strong>{counts.products}</strong></div><div className="stat"><span><ShoppingCart size={15}/> Pedidos</span><strong>{counts.orders}</strong></div><div className="stat"><span><Users size={15}/> Usuários</span><strong>{counts.users}</strong></div><div className="stat"><span>Status</span><strong>{store.subscriptionStatus||'trial'}</strong></div></div><div className="two-cols"><div className="panel"><h2>Dados da loja</h2><div className="detail-list simple"><div><span>Responsável</span><strong>{store.ownerEmail||'—'}</strong></div><div><span>Link</span><strong>/loja/{store.slug}</strong></div><div><span>Assinatura</span><strong>Mensal</strong></div><div><span>Acesso</span><strong>{store.active?'Liberado':'Bloqueado'}</strong></div></div></div><div className="panel"><h2>Modo suporte seguro</h2><p className="muted">Esta área permite ao Master consultar informações operacionais sem se autenticar como o cliente. Isso evita compartilhamento de senha ou impersonação insegura.</p></div></div></>}
